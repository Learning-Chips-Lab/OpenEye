version: 2

build:
  os: ubuntu-24.04
  apt_packages:
    - libgirepository1.0-dev
    - libcairo2-dev
  tools:
    python: "3.12"

sphinx:
  configuration: doc/source/conf.py

python:
  install:
    - requirements: doc/requirements.txt
